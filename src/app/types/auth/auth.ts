import type { ReactNode } from 'react';

export type loginType = {
  title?: string;
  subtitle?: ReactNode;
  subtext?: ReactNode;
};

export type registerType = {
  title?: string;
  subtitle?: ReactNode;
  subtext?: ReactNode;
};

export type signInType = {
  title?: string;
};
