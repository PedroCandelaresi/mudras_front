# Variables de entorno locales para el frontend de Mudras
# Copia este archivo a `.env.local` y ajusta los valores según tu entorno.

# URL del backend NestJS (REST y GraphQL)
# En desarrollo el backend corre en el puerto 4000 (ver backend/src/main.ts)
BACKEND_URL=http://localhost:4000
# Alternativamente puedes usar la variable pública si necesitas acceder desde el cliente
# NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Clave opcional para proteger llamadas internas (si tu backend valida X-Secret-Key)
# NEXT_PUBLIC_X_SECRET_KEY=
